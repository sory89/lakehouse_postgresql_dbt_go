name: lakehouse
version: "1.0.0"
config-version: 2

profile: lakehouse

model-paths:   ["models"]
test-paths:    ["tests"]
macro-paths:   ["macros"]

target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  lakehouse:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table
      +tags: ["gold", "analytics"]
